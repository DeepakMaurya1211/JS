"use strict"; // treat all JS code as newer version

let name = "Vulcan" // string type
let age = 18 // Integer type
let loggedIn = true  //boolean type


// null => standalone value (representation of an empty value)
// undefined => variable declared but no defined value
//symbol => unique

// object
console.log(typeof undefined);
